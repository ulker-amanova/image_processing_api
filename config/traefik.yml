entryPoints:
  web:
    address: ":80"

api:
  dashboard: true

providers:
  file:
    filename: /etc/traefik/dynamic.yml

http:
  routers:
    api:
      rule: "Host(`traefik.localhost`)"
      service: api@internal
      entryPoints:
        - web
